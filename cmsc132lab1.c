#include <stdio.h>

int main() {

int x;
int y;
int sum;
int R0;

printf("Enter first digit (R1) ");
scanf("%d", &x);

printf("Enter second digit (R2) ");
scanf("%d", &y);

sum = x + y;
R0 = sum;

printf("The sum of two numbers is %d", R0);
//return R0;

}
